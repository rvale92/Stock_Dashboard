// Application constants

// Poll interval for refreshing stock data (65 seconds to respect API limits)
export const POLL_MS = 65000;

// Stagger delay between initial symbol fetches (1.5 seconds)
export const STAGGER_DELAY_MS = 1500;

